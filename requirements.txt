# ==========================================
# 智能金融投研 RAG 系统 - 核心依赖清单
# Python 版本要求: >= 3.10
# ==========================================

# 1. 核心编排与大模型接入 (LangChain & Qwen)
langchain>=0.1.16
langchain-core>=0.1.45
langchain-community>=0.0.34
dashscope>=1.17.0              # 阿里通义千问官方 API SDK
langchain_openai>=0.1.3

# 2. 脏数据治理与版面分析 (Parsing)
docling>=1.16.0                # 核心解析器，精准提取 PDF 表格与 Markdown
pypdf>=3.17.0                  # 轻量级 PDF 页数读取工具，用于 OOM 防御

# 3. 向量数据库与 Embedding (Storage)
pymilvus[model]>=2.4.0         # 必须加上 [model] 后缀，以支持本地直接计算 BM25 稀疏向量
jieba>=0.42.1                  # 用于给 BM25 提供精确的中文切词支持

# 4. 后端服务化 (FastAPI Backend)
fastapi>=0.110.1               # 异步 Web 框架
uvicorn[standard]>=0.29.0      # 高性能 ASGI 服务器
pydantic>=2.7.0                # 数据验证与序列化
pydantic-settings>=2.2.1       # 环境变量配置管理
python-multipart>=0.0.9        # 🚨 必加：FastAPI 处理文件上传的核心依赖

# 5. HTTP 客户端
requests>=2.31.0

# 6. 数据处理与基础工具箱 (Utils)
pandas>=2.2.2                  # 结构化数据处理
numpy>=1.26.4                  # 基础数值计算
python-dotenv>=1.0.1           # 读取 .env 环境变量文件

# 7. 关系型数据库驱动与 ORM 框架 (PostgreSQL)
SQLAlchemy==2.0.29
psycopg2-binary==2.9.9         # 专为容器环境准备的二进制驱动，无需编译

# 8. Agent 编排框架与文本处理
langgraph>=0.0.30
langchain-text-splitters>=0.0.1

# 9. Prompt 模板加载
pyyaml>=6.0

# 10. 测试框架
pytest>=8.0
pytest-mock>=3.12